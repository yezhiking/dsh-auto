# Runtime data - never commit (logs, local configs, pid files, tools, staging)
data/
logs/
tools/
.update-staging/

# Local per-machine config files
harness-port-config.txt
harness-options.txt
harness-config.json

# Logs and runtime markers
*.log
*.pid
*.marker
*.lnk

# Node toolchain (if a portable node was installed)
node.exe
node-v*-win-x64/
